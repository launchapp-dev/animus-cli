workflows:
  - id: builtin/requirements-execute
    name: Builtin Requirements Execute
    description: Dispatch requirement subjects into the shipped task-generation-and-bootstrap workflow.
    phases:
      - workflow_ref: requirement-task-generation-run
