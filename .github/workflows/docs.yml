name: Build Docs Site

on:
  push:
    branches: [main]
    paths:
      - 'Cargo.toml'
      - 'docs/**'
      - 'package.json'
      - 'package-lock.json'
      - 'vercel.json'
      - 'scripts/check-doc-sync.sh'
      - 'crates/orchestrator-cli/src/cli_types/root_types.rs'
      - 'crates/orchestrator-cli/src/services/operations/ops_mcp.rs'
      - 'crates/orchestrator-cli/src/services/operations/ops_mcp/**'
      - '.github/workflows/docs.yml'
  pull_request:
    paths:
      - 'Cargo.toml'
      - 'docs/**'
      - 'package.json'
      - 'package-lock.json'
      - 'vercel.json'
      - 'scripts/check-doc-sync.sh'
      - 'crates/orchestrator-cli/src/cli_types/root_types.rs'
      - 'crates/orchestrator-cli/src/services/operations/ops_mcp.rs'
      - 'crates/orchestrator-cli/src/services/operations/ops_mcp/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

env:
  # GitHub Actions runner Node 20 -> Node 24 deprecation (effective 2026-06-02).
  # Forces JS-based actions onto Node 24 ahead of the cutover.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run docs:check-sync

      - run: npm run docs:build
